<%= form_for(@expense) do |f| %>
    <%= f.label :amount %>
    <%= f.number_field :amount, options: { precision: 2 } %>
    <%= f.label :description %>
    <%= f.text_field :description %>
    <%= f.submit "Add Expense" %>
<% end %>